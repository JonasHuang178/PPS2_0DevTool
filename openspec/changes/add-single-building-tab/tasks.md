## 1. 外殼：每個功能各自的來源路徑

- [ ] 1.1 在外殼中加入以功能名稱為鍵的來源路徑保管（`QMap<QString, QString>`），並在程式碼中註明它只存在於執行期間、不寫入設定檔；以編譯通過驗證
- [ ] 1.2 把 `sourcePathChanged` 的簽章改為附帶功能名稱，並在註解中說明為何需要它（Qt 訊號會送達所有 slot，功能端必須自行過濾）；以編譯通過驗證
- [ ] 1.3 在分頁切換時把該功能的來源路徑寫回共用文字框，並同步更新去重用的前值使訊號不發出；在程式碼中註明該成員變數的語意已從「上一次的路徑」變為「當前功能的路徑」
- [ ] 1.4 手動驗證：暫時建立兩個測試 tab，在 A 設路徑後切到 B 改路徑，切回 A 應還原 A 的路徑，且切換過程中不觸發任何重新載入（以 Debug console 的訊號紀錄確認）
- [ ] 1.5 更新 `README.md` 中「新增一個功能的步驟」，說明來源路徑現在是每個功能各自保有

## 2. 腳本：共用模組

- [ ] 2.1 建立 `scripts/script_utils/single_building/` 模組，含決定暫存設定檔位置的單一函式（`tempfile.gettempdir()` + 固定檔名）；以 Python 直接呼叫該函式印出路徑驗證，Windows 上應落在 `%TEMP%` 下
- [ ] 2.2 在該模組實作掃描目錄該層 `.cpp` 的函式（不遞迴），回傳 `name` 與 `path` 兩欄；以一個含子目錄與非 `.cpp` 檔案的暫存目錄驗證只取得該層的 `.cpp`
- [ ] 2.3 在該模組實作暫存設定檔的讀取、寫入與清空三個函式；驗證檔案不存在時讀取回傳空清單而非拋出例外，且寫入後讀回的內容與寫入相同

## 3. 腳本：四支入口

- [ ] 3.1 複製 `_function_template.py` 建立「取得來源清單」腳本，宣告所需設定鍵與參數；以 `--help` 與 `--dump-config` 驗證自我描述正確，並以 `--request` 餵入含 `source_path` 的信封驗證回傳 `data.files`
- [ ] 3.2 複製 `_function_template.py` 建立「讀取設定」腳本；以 `--request` 驗證回傳 `data.files`，且設定檔不存在時回傳 `PASS` 與空清單
- [ ] 3.3 複製 `_function_template.py` 建立「寫入設定」腳本，從 `params` 接收絕對路徑清單；以 `--request` 驗證寫入後檔案內容為每行一個絕對路徑，且覆蓋原有內容
- [ ] 3.4 複製 `_function_template.py` 建立「清空設定」腳本，清空後回讀並回傳結果；以 `--request` 驗證回傳的 `data.files` 為空且檔案內容已清空
- [ ] 3.5 驗證四支腳本的 stdout 都只有單一 JSON 物件，診斷訊息全走 stderr（以 `python <script> --request <file> 1>out.json 2>err.txt` 分離後檢查 `out.json` 可被 JSON 解析）

## 4. Qt 端：Single Building 功能

- [ ] 4.1 在 `pps2_0devtool.ui` 加入 `Single Building` 分頁與其全部元件（兩個群組、過濾列、兩個清單、兩顆搬移鈕、兩顆動作鈕）；以 Qt Designer 預覽確認版面與 spec 的配置一致
- [ ] 4.2 建立 `SingleBuilding.{h,cpp}`，建構子以 `getFunctionConfig()` 取得自己的設定區塊；以編譯通過驗證
- [ ] 4.3 實作兩個清單的模型與排序過濾代理，含自然排序與不區分大小寫的排序規則；以含 `a1/a2/a10` 與混合大小寫的資料驗證次序符合 spec 的三個排序場景
- [ ] 4.4 實作來源清單的過濾：區分大小寫的子字串比對、即時生效、文字變動時清空選取、清除鈕只清文字且為空時停用；逐項對照 spec 的過濾場景驗證，含萬用字元被當成一般字元
- [ ] 4.5 實作搬移行為：`->` 複製並靜默略過重複、`<-` 移除、兩個清單皆可雙擊、無選取時對應按鈕停用；逐項對照 spec 的加入與移出場景驗證
- [ ] 4.6 實作結果清單的清除鈕（清空畫面、不碰設定檔、為空時停用）；驗證清空後未按 Modify Setting 就重新進入功能時，內容回復為設定檔的內容
- [ ] 4.7 實作清單項目的顯示與保存形式：顯示檔名、項目附帶絕對路徑、重複判斷依檔名；以來源目錄含同名不同路徑的情境驗證寫入的是絕對路徑
- [ ] 4.8 實作腳本串接的共用機制（前一支的 callback 中啟動下一支，取消即中止整串）；以取消「取得來源清單」驗證讀取設定不被啟動且兩個清單維持原狀
- [ ] 4.9 實作進入功能時的載入，含初始化階段的明確呼叫；驗證應用程式啟動且此分頁為當前分頁時內容即已載入，不需切走再切回
- [ ] 4.10 實作來源路徑為空時略過取得來源清單的分支；驗證啟動時不出現錯誤訊息框，且讀取設定仍照常進行
- [ ] 4.11 實作來源路徑變更時的「重新開始」（重用清空設定的腳本後重新取得來源清單），並以功能名稱過濾非本功能的訊號；驗證在其他分頁變更路徑時本功能的設定檔未被清空
- [ ] 4.12 實作 Modify Setting；驗證寫入後檔案內容與結果清單一致，結果清單為空時寫入空內容且不報錯，成功後不重新讀取設定檔
- [ ] 4.13 實作 Recovery Setting，含確認對話框；驗證確認後設定檔清空且結果清單為空，取消時設定檔與兩個清單皆不變
- [ ] 4.14 實作失敗處理：對應清單清空並顯示原因，同一次載入中兩者皆失敗時只顯示一個合併的訊息框；以不存在的來源目錄與不可讀的設定檔分別驗證

## 5. 設定與建置

- [ ] 5.1 在 `PPS2_0DevTool.json` 的 `Function` 底下新增 `Single Building` 區塊（含 `Visible` 與 `Program`）；驗證 `Visible` 為 `"false"` 時分頁被移除、為 `"true"` 時保留
- [ ] 5.2 把新增的 `.h` / `.cpp` 加入 `PPS2_0DevTool.pro` 的 `HEADERS` / `SOURCES`；以 Windows（Qt Creator + MinGW）與 macOS 兩邊各建置一次驗證

## 6. 驗收

- [ ] 6.1 在 Windows 上完整走一次流程：選來源路徑 → 挑檔案 → Modify Setting → 重啟程式確認結果清單自設定檔還原 → Recovery Setting 確認清空
- [ ] 6.2 補驗外殼延後的項目一：`Debug_Mode` 開啟時，於腳本執行中關閉 Debug console，確認子行程被終止且系統中無殘留（工作管理員確認）
- [ ] 6.3 補驗外殼延後的項目二：`Debug_Mode` 開啟時執行腳本，確認 Qt 端與 Python 端的訊息同時出現在同一個 console 且格式對齊、中文不亂碼
- [ ] 6.4 更新 `README.md`：移除「尚未包含任何功能 tab」的敘述，並記錄 6.2 與 6.3 兩項延後驗收已完成
- [ ] 6.5 執行 `openspec validate add-single-building-tab --strict` 確認規格與實作一致後，交由使用者決定是否歸檔
